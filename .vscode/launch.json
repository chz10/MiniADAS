{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug MiniADAS",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/mini_adas_eval",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "MiniADAS: build"
        }
    ]
}
